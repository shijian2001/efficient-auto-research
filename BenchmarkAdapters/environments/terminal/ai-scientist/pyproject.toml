[project]
name = "terminal-bench-ai-scientist"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "aiscientist",
  "harbor==0.20.0",
]

[dependency-groups]
test = [
  "pytest>=8.4,<9",
]

[tool.uv]
package = false

[tool.uv.sources]
aiscientist = { path = "../../../../baselines/AiScientist", editable = true }
