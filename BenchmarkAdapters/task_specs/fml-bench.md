# FML-Bench Same-Model Agent Framework Track

Follow the pinned upstream FML-Bench task, round, proposal, validation, and final-test semantics without adapter-side search or candidate repair. Modify only the task-declared target files in the isolated workspace. The adapter records every upstream task-level evaluator result and hashes the final artifact. Upstream internal rounds and proposals are distinct from outer framework repetitions.
