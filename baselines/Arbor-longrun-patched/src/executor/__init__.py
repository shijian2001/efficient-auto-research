"""Sub-agent — the research executor that implements individual ideas.

Given a codebase and a research idea, the executor faithfully implements
the idea, runs experiments, and reports results. It never decides what to
try — only implements what it's told.
"""
